class AppDimensions {
  AppDimensions._();

  // Sharp, near-zero-radius instrument-panel edges — shared by both the
  // Carbon Mono and Editorial palettes; shape doesn't change with theme.
  static const double radiusSm = 2.0;
  static const double radiusMd = 2.0;
  static const double radiusLg = 4.0;
  static const double radiusXl = 6.0;
  static const double radiusFull = 4.0;

  static const double paddingSm = 8.0;
  static const double paddingMd = 16.0;
  static const double paddingLg = 24.0;
  static const double paddingXl = 32.0;

  static const double bottomNavHeight = 72.0;
  static const double appBarHeight = 64.0;
}
