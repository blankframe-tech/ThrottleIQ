import 'package:flutter_test/flutter_test.dart';

void main() {
  // Smoke test placeholder — full tests require Firebase mocking
  test('ThrottleIQ app test placeholder', () {
    expect(true, isTrue);
  });
}
